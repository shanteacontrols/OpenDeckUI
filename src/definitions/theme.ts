const paddingMain = `p-4 md:px-6 lg:px-8 xl:px-10`;

export const defaultTheme = {
  box: `${paddingMain}`,
  section: `bg-black`,
  sectionTitle: `${paddingMain} font-bold border-b border-gray-900 text-gray-400`,
  sectionContent: `${paddingMain}`,
};
